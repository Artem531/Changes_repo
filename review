1 - после функции get_data(path_to_velodyne) цикл:

names = get_data(path_to_velodyne)


for idx, frame in enumerate(frame_data):
    result_str = ''
    for list_info in frame_data[frame]:
        list_info = map(str, list_info)
        #print(list_info)
        s1 = ' '.join(list_info)
        result_str += s1 + '\n'
    with open(path + names[idx] + ".txt", 'w') as outfile:
       . outfile.write(result_str)
    name += 1 
-> что такое это name???????????

2 - c путями какая-то вообще беда. Стоит где-то в начале или структуру ожидаемую прописать или еще как-то 

правильно ли я понимаю, что в ноутбуке  толкьо цикл, который вверху написан?
